/**
 * Customizer Preview - Entry point
 *
 * Imports and initializes the preview bindings module.
 * Loaded in the preview iframe via customize_preview_init.
 *
 * @package CT_Custom
 */

import { init } from './preview/customizer-preview.js';

init();
